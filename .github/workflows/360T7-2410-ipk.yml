name: 360T7-2410-ipk

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout
      uses: actions/checkout@main

    - name: Initialization environment
      run: sudo bash -c 'bash <(curl -s https://build-scripts.immortalwrt.org/init_build_environment.sh)'

    - name: download
      run: git clone -b openwrt-24.10-6.6 --single-branch --filter=blob:none https://github.com/padavanonly/immortalwrt-mt798x-6.6 immortalwrt-mt798x

    - name: update
      run: cd immortalwrt-mt798x && ./scripts/feeds update -a

    - name: install
      run: cd immortalwrt-mt798x && ./scripts/feeds install -a

    - name: cp
      run: cd immortalwrt-mt798x && cp -f $GITHUB_WORKSPACE/config/360T7_2410_ipk.config .config

    - name: passwall
      run: |
        cd immortalwrt-mt798x
        # 移除 openwrt feeds 自带的核心库
        rm -rf feeds/packages/net/{xray-core,v2ray-geodata,sing-box,chinadns-ng,dns2socks,hysteria,ipt2socks,microsocks,naiveproxy,shadowsocks-libev,shadowsocks-rust,shadowsocksr-libev,simple-obfs,tcping,trojan-plus,tuic-client,v2ray-plugin,xray-plugin,geoview,shadow-tls}
        git clone https://github.com/Openwrt-Passwall/openwrt-passwall-packages package/passwall-packages
        # 移除 openwrt feeds 过时的luci版本
        rm -rf feeds/luci/applications/luci-app-passwall
        git clone https://github.com/Openwrt-Passwall/openwrt-passwall package/passwall-luci
        # 替换规则
        sed -i '/gfwlist\/gfwlist"/a\o:value("https://github.com/najloa/geoip/releases/latest/download/proxy.txt", translate("najloa/proxy"))' ./package/passwall-luci/luci-app-passwall/luasrc/model/cbi/passwall/client/rule.lua
        sed -i '/ChinaMax_Domain"/a\o:value("https://github.com/najloa/geoip/releases/latest/download/twitchcdn.txt", translate("najloa/twitch-cdn"))' ./package/passwall-luci/luci-app-passwall/luasrc/model/cbi/passwall/client/rule.lua
        sed -i '/ChinaMax_Domain"/a\o:value("https://github.com/najloa/geoip/releases/latest/download/cn.txt", translate("najloa/cn"))' ./package/passwall-luci/luci-app-passwall/luasrc/model/cbi/passwall/client/rule.lua
        sed -i '/MetaCubeX\/geosite (CDN)"/a\	o:value("https://github.com/najloa/geoip/releases/latest/download/geosite.dat", translate("najloa/geosite"))' ./package/passwall-luci/luci-app-passwall/luasrc/model/cbi/passwall/client/rule.lua
        sed -i '/MetaCubeX\/geoip (CDN)"/a\	o:value("https://github.com/najloa/geoip/releases/latest/download/geoip.dat", translate("najloa/geoip"))' ./package/passwall-luci/luci-app-passwall/luasrc/model/cbi/passwall/client/rule.lua
        sed -i 's|local excluded_domain = {[^}]*}|local excluded_domain = {}|g' ./package/passwall-luci/luci-app-passwall/root/usr/share/passwall/rule_update.lua
        curl -s https://core.telegram.org/resources/cidr.txt > ./package/passwall-luci/luci-app-passwall/root/usr/share/passwall/rules/proxy_ip

    - name: go
      run: |
        cd immortalwrt-mt798x && rm -rf ./feeds/packages/lang/golang && git clone --depth 1 https://github.com/sbwml/packages_lang_golang -b 26.x feeds/packages/lang/golang

    - name: package
      run: |
        cd immortalwrt-mt798x
        make defconfig
        make download -j8
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: Compile the firmware
      run: |
        cd immortalwrt-mt798x
        make -j$(nproc) || make -j1 || make -j1 V=s

    - name: Locate .ipk
      run: |
        mkdir -p ipk-out
        find ./ -type f -name '*passwall*.ipk' -exec cp {} ipk-out/ \;
        find ./ -type f -name '*smartdns*.ipk' -exec cp {} ipk-out/ \;
        find ./ -type f -name '*chinadns-ng*.ipk' -exec cp {} ipk-out/ \;
        find ./ -type f -name '*dns2socks*.ipk' -exec cp {} ipk-out/ \;
        find ./ -type f -name '*geoview*.ipk' -exec cp {} ipk-out/ \;
        find ./ -type f -name '*ipt2socks*.ipk' -exec cp {} ipk-out/ \;
        find ./ -type f -name '*microsocks*.ipk' -exec cp {} ipk-out/ \;
        find ./ -type f -name '*tcping*.ipk' -exec cp {} ipk-out/ \;
        find ./ -type f -name '*xray*.ipk' -exec cp {} ipk-out/ \;
        find ./ -type f -name '*sing-box*.ipk' -exec cp {} ipk-out/ \;

    - name: Get latest release tag
      id: get_latest
      run: |
        response=$(curl -s https://api.github.com/repos/${{ github.repository }}/releases/latest)
        tag=$(echo "$response" | jq -r .tag_name)

        if [ "$tag" = "null" ] || [ -z "$tag" ]; then
          echo "No release found"
          exit 1
        fi

        echo "tag=$tag" >> $GITHUB_OUTPUT

    - name: Pack IPK files (zip)
      run: |
        cd ipk-out
        zip -r ../ipk2410.zip .

    - name: Upload to latest release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ steps.get_latest.outputs.tag }}
        files: ipk2410.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Delete workflow runs
      uses: Mattraks/delete-workflow-runs@main
      with:
        retain_days: 30
        keep_minimum_runs: 6
        delete_workflow_pattern: 360T7-2410-ipk
